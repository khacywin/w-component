import { css } from 'styled-components';

const normal = css`border-radius: 7px;`;
const menu = css`border-radius: 10px;`;
const around = css`border-radius: 9999px;`;

export {
  normal,
  around,
  menu
};